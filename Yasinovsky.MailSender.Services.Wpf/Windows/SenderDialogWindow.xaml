<Window x:Class="Yasinovsky.MailSender.Services.Wpf.Windows.EmailAddressDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Yasinovsky.MailSender.Services.Wpf.Windows"
        mc:Ignorable="d"
        Title="EmailAddressDialogWindow" Height="450" Width="450">
    <StackPanel Margin="20,10">
        <TextBlock Text="Имя" />
        <TextBox Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="Адрес" />
        <TextBox Text="{Binding Path=Address, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="Описание" />
        <TextBox
            Height="75"
            AcceptsReturn="True"
            AcceptsTab="True"
            AllowDrop="True"
            Text="{Binding Path=Description, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="WrapWithOverflow" />
        <StackPanel>
            <Button Click="DialogSaveClose" Content="Сохранить" />
            <Button Click="DialogCancelClose" Content="Отмена" />
        </StackPanel>
    </StackPanel>
</Window>
